# rust-api
A simple REST api written in RUST with ORM support using diesel and r2d2-diesel for connection pooling.

The application can also be deployed using docker.

This api only compile using rust-nightly
